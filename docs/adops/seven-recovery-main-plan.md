# Seven campaign recovery / main integration

## Global constraints

Preserve existing production behavior, audited evidence and dirty worktrees. No force push, sheet writes, folder creation/renaming, sibling cancellation or fabricated historical dates. Scope PI 3218, 3219, 42061, 9773, 17408, 91381, 9772. Revalidate live sources before mutation. Fail closed per target on identity, format, media, period or audit conflicts.

## Task 1: Reconcile production lineage

Start from current origin/main. Merge active production recovery only after inspecting net tree; retain main report panel URLs. Run existing regressions and independent diff review.

## Task 2: Targeted drive-pi-publish recovery

Reuse existing API and runner. Explicit exact target and fresh sheet/PDF agreement required. Missing insertion may be created only when unique; existing insertion updated in place. Validate media bytes/type/dimensions/checksum before materialization. No global sync or sibling cancellation. Only pending past/current evidence dates, no approved overwrite, per-date result and incomplete repair failure. Test all seven scenarios, ambiguity, duplicate, invalid media, date gates, and idempotency.

## Task 3: Production and operational acceptance

Review isolated diff, PR/merge to main, restore-tested backup, deploy merged SHA via existing Portainer flow. Process authorized targets serially, monitor each job to terminal. Validate relation/publication/provenance/audit, regenerate report and capture real authenticated browser. Document exact residual blockers; never claim reconstructed evidence proves historic delivery.
